<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 🌐 Internet and Location Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bidzi"
        tools:targetApi="31">
        <activity
            android:name=".notification.NotificationManagementActivity"
            android:exported="false" />
        <!-- 🎨 SplashActivity (Entry Point) -->
        <activity
            android:name=".auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Bidzi.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 🏠 MainActivity (After Splash) -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true" /> <!-- 🔐 Auth Activity -->
        <activity
            android:name=".auth.AuthActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" /> <!-- 🚗 Ride Request Activity -->
        <activity
            android:name=".activity.RideRequestActivity"
            android:exported="false" /> <!-- 🗺️ Map Activity -->
        <activity
            android:name=".MapActivity"
            android:exported="false" /> <!-- 🗺️ Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>